export const drawLines = (map, country) => {
  // Port the original drawLines function
  // Implement line drawing logic using Leaflet
}

export const clearLines = (map) => {
  // Port the original clearLines function
  // Implement line clearing logic
}

export const calculateWeight = (partners, value) => {
  // Port the original calculateWeight function
  return Math.min(14, Math.max(2, /* calculation */))
}

export const calculateRadius = (partners, value) => {
  // Port the original calculateRadius function
  return Math.min(35000, Math.max(20000, /* calculation */))
} 